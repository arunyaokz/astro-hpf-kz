---
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>HPF.kz | 跨里海中间走廊 · 确定性供应链平台</title>
    <meta name="description" content="基于TITR中间走廊、TIR超级卡车与库雷克港绿色通道，提供面向欧洲的端到端确定性多式联运解决方案。" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <style>
      :root {
        --bg: #0b0e14; /* 墨蓝 */
        --card: #0f1320;
        --text: #eaf2ff;
        --muted: #a0aec0;
        --accent: #4da3ff;
        --accent-2: #7bd7c8;
        --border: #20263a;
      }
      html, body { margin:0; padding:0; background: var(--bg); color: var(--text); font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Noto Sans, PingFang SC, "Hiragino Sans GB", "Microsoft YaHei", sans-serif; }
      a { color: var(--accent); text-decoration: none; }
      .container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }
      /* 顶部导航 */
      .nav { position: sticky; top:0; z-index: 50; backdrop-filter: blur(10px); background: rgba(11,14,20,0.6); border-bottom: 1px solid var(--border); }
      .nav-inner { display:flex; align-items:center; justify-content:space-between; height:64px; }
      .brand { font-weight:800; letter-spacing: .5px; }
      .brand b { color: var(--accent); }
      .nav a.btn { padding:10px 16px; border-radius:999px; border:1px solid var(--accent); }
      /* 英雄区 */
      .hero { padding: 80px 0 40px; }
      .hero-grid { display:grid; grid-template-columns: 1.2fr .8fr; gap: 40px; align-items:center; }
      .eyebrow { color: var(--accent-2); font-weight:700; letter-spacing:.1em; text-transform: uppercase; font-size:12px; }
      .title { font-size: clamp(32px, 6vw, 56px); line-height:1.1; margin: 12px 0 12px; }
      .subtitle { color: var(--muted); font-size: clamp(16px, 2.2vw, 20px); }
      .hero-cta { display:flex; gap:12px; margin-top: 24px; }
      .btn { display:inline-flex; align-items:center; gap:8px; padding:12px 18px; border-radius:12px; border:1px solid var(--border); color:var(--text); }
      .btn-primary { background: linear-gradient(135deg, #2b68ff 0%, #00b7a8 100%); border-color: transparent; }
      .btn-outline { background: transparent; }
      .hero-card { background: linear-gradient(180deg, rgba(77,163,255,0.08) 0%, rgba(123,215,200,0.06) 100%); border:1px solid var(--border); border-radius:16px; padding: 18px; }
      .hero-card h4 { margin: 0 0 8px; font-size: 14px; color: var(--muted); }
      .kpis { display:grid; grid-template-columns: repeat(4,1fr); gap: 12px; margin-top: 8px; }
      .kpi { background: rgba(255,255,255,0.03); border:1px solid var(--border); border-radius:12px; padding:12px; text-align:center; }
      .kpi b { font-size: 24px; color: var(--accent); }
      /* 价值主张 */
      section { padding: 56px 0; }
      .section-title { font-size: 28px; margin-bottom: 20px; }
      .pill { display:inline-block; padding:6px 10px; border:1px solid var(--border); border-radius:999px; color:var(--muted); font-size:12px; }
      .grid-3 { display:grid; grid-template-columns: repeat(3,1fr); gap:16px; }
      .card { background: var(--card); border:1px solid var(--border); border-radius:16px; padding:20px; }
      .card h3 { font-size: 18px; margin: 6px 0 8px; }
      .muted { color: var(--muted); }
      /* 路线与SLA */
      .route { display:grid; grid-template-columns: 1.2fr .8fr; gap: 24px; }
      .route-line { background: var(--card); border:1px dashed var(--accent); border-radius:14px; padding: 16px; }
      .steps { display:flex; align-items:center; justify-content:space-between; gap:8px; }
      .step { text-align:center; font-size:12px; color: var(--muted); }
      .dot { width:10px; height:10px; border-radius:50%; background: var(--accent); margin: 8px auto; }
      .dash { flex:1; height:2px; background: linear-gradient(90deg, transparent 0, var(--accent) 20%, transparent 40%); opacity:.5; }
      .sla { display:grid; grid-template-columns: repeat(3,1fr); gap: 10px; }
      .sla .card { text-align:center; }
      .sla b { font-size: 18px; color: var(--accent-2); }
      /* 产品服务 */
      .grid-4 { display:grid; grid-template-columns: repeat(4,1fr); gap: 16px; }
      /* 联系表单 */
      .contact { display:grid; grid-template-columns: 1fr 1fr; gap: 24px; }
      input, textarea, select { width:100%; background: #0c1120; color: var(--text); border:1px solid var(--border); border-radius:12px; padding:12px; }
      label { display:block; font-size:12px; color: var(--muted); margin-bottom:6px; }
      footer { border-top:1px solid var(--border); padding: 24px 0; color: var(--muted); }
      /* 响应式 */
      @media (max-width: 900px) {
        .hero-grid, .route, .contact, .grid-4 { grid-template-columns: 1fr; }
        .grid-3 { grid-template-columns: 1fr; }
        .kpis { grid-template-columns: repeat(2,1fr); }
      }
    </style>

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "HPF.kz",
      "description": "基于TITR中间走廊、TIR超级卡车与库雷克港绿色通道，提供面向欧洲的端到端确定性多式联运解决方案。",
      "url": "https://hpf.kz",
      "sameAs": ["https://www.linkedin.com/company/hpfkz"],
      "contactPoint": [{"@type": "ContactPoint","contactType": "sales","email": "contact@hpf.kz"}]
    }
    </script>
  </head>
  <body>
    <header class="nav">
      <div class="container nav-inner">
        <div class="brand">HPF.<b>kz</b></div>
        <nav style="display:flex; gap:16px; align-items:center;">
          <a href="#value">价值</a>
          <a href="#route">走廊</a>
          <a href="#products">产品</a>
          <a href="#cases">案例</a>
          <a class="btn" href="#contact">获取方案</a>
        </nav>
      </div>
    </header>

    <main>
      <!-- Hero -->
      <section class="hero">
        <div class="container hero-grid">
          <div>
            <div class="eyebrow">TITR · KURYK GREEN CHANNEL · TIR SUPER TRUCK</div>
            <h1 class="title">跨里海中间走廊 · 确定性供应链平台</h1>
            <p class="subtitle">避开高风险海域与北部走廊不确定性，以TIR超级卡车+库雷克绿通+多式联运构建15–18天直达欧洲的稳定路径，对“带电产品/整车/高价值SKU”友好且合规。</p>
            <div class="hero-cta">
              <a class="btn btn-primary" href="#contact">获取定制方案</a>
              <a class="btn btn-outline" href="#route">查看路线与SLA</a>
            </div>
          </div>
          <div class="hero-card">
            <h4>关键指标（Service-level Targets）</h4>
            <div class="kpis">
              <div class="kpi"><b>99%</b><div class="muted">准时率目标</div></div>
              <div class="kpi"><b>99.2%</b><div class="muted">完损率</div></div>
              <div class="kpi"><b>12–18天</b><div class="muted">门到门时效</div></div>
              <div class="kpi"><b>T+0</b><div class="muted">异常预警</div></div>
            </div>
          </div>
        </div>
      </section>

      <!-- 价值主张 -->
      <section id="value">
        <div class="container">
          <div class="pill">端到端确定性 · 可视 · 合规 · 绿色</div>
          <h2 class="section-title">为什么选择HPF.kz</h2>
          <div class="grid-3">
            <div class="card"><h3>库雷克绿色通道</h3><p class="muted">专用窗口与驳船优先级，减少靠泊与等待波动，提升跨海段可预测性。</p></div>
            <div class="card"><h3>TIR超级卡车</h3><p class="muted">跨境不落地海关检查，支持整车/CKD/SKD与危险品（锂电等）合规运输。</p></div>
            <div class="card"><h3>多式联运编排</h3><p class="muted">公-铁-海灵活组合，黑海/里海冗余，动态绕行保障在地缘波动期持续履约。</p></div>
          </div>
        </div>
      </section>

      <!-- 路线与SLA -->
      <section id="route">
        <div class="container route">
          <div class="route-line">
            <div class="steps">
              <div class="step">中国<div class="dot"></div></div>
              <div class="dash"></div>
              <div class="step">哈萨克斯坦<br/>（库雷克港）<div class="dot"></div></div>
              <div class="dash"></div>
              <div class="step">阿塞拜疆<div class="dot"></div></div>
              <div class="dash"></div>
              <div class="step">格鲁吉亚<div class="dot"></div></div>
              <div class="dash"></div>
              <div class="step">土耳其<div class="dot"></div></div>
              <div class="dash"></div>
              <div class="step">欧盟<div class="dot"></div></div>
            </div>
          </div>
          <div>
            <h2 class="section-title">标准SLA</h2>
            <div class="sla">
              <div class="card"><div class="muted">标准</div><b>15–18天</b></div>
              <div class="card"><div class="muted">加急</div><b>12–15天</b></div>
              <div class="card"><div class="muted">绿通</div><b>10–12天</b></div>
            </div>
            <p class="muted" style="margin-top:10px">注：时效依据节点拥堵、口岸气象与舱位窗口动态调整，提供“确定性保险”方案可覆盖延误风险。</p>
          </div>
        </div>
      </section>

      <!-- 产品与服务 -->
      <section id="products">
        <div class="container">
          <h2 class="section-title">产品与服务</h2>
          <div class="grid-4">
            <div class="card"><h3>库雷克绿通</h3><p class="muted">优先靠泊/驳船排期、口岸联动、异常分流预案。</p></div>
            <div class="card"><h3>TIR公路直达</h3><p class="muted">跨境不落地通关，适配带电/整车等高价值货类。</p></div>
            <div class="card"><h3>铁路联运</h3><p class="muted">中欧班列衔接无水港，编组/解编与时窗优化。</p></div>
            <div class="card"><h3>黑/里海航段</h3><p class="muted">多港冗余（库雷克/巴库/波季），舱位与靠泊资源统筹。</p></div>
            <div class="card"><h3>清关与合规</h3><p class="muted">EAEU/TR CU/欧盟合规模板与一次性合规设计。</p></div>
            <div class="card"><h3>仓配与末端</h3><p class="muted">保税/温控、门店/经销直达，NPS闭环评估。</p></div>
            <div class="card"><h3>可视与风控</h3><p class="muted">IoT追踪、温震监控、AI路由与T+0预警。</p></div>
            <div class="card"><h3>金融增值</h3><p class="muted">保险/结算/汇率优化，现金流周转提升。</p></div>
          </div>
        </div>
      </section>

      <!-- 案例与行业 -->
      <section id="cases">
        <div class="container">
          <h2 class="section-title">行业场景</h2>
          <div class="grid-4">
            <div class="card"><h3>新能源与带电品</h3><p class="muted">整车/电池/逆变器合规运输，CBAM与ESG对齐。</p></div>
            <div class="card"><h3>工程机械</h3><p class="muted">超尺许可、道路测绘、项目工地一体化交付。</p></div>
            <div class="card"><h3>跨境电商</h3><p class="muted">中高价值SKU时效/成本平衡，逆向闭环。</p></div>
            <div class="card"><h3>快消零售</h3><p class="muted">高频补货与温控协同，渠道分拨策略。</p></div>
          </div>
        </div>
      </section>

      <!-- 联系我们 -->
      <section id="contact">
        <div class="container contact">
          <div>
            <h2 class="section-title">获取定制化方案</h2>
            <p class="muted">请留下您的需求，包含货类（如带电/整车/超尺）、起讫地、时效目标与预计周/月度量。</p>
            <form name="contact" method="POST" data-netlify="true">
              <input type="hidden" name="form-name" value="contact" />
              <div><label>公司/联系人</label><input name="name" required placeholder="如：某新能源出口负责人"/></div>
              <div style="margin-top:12px"><label>邮箱</label><input type="email" name="email" required placeholder="you@company.com"/></div>
              <div style="margin-top:12px"><label>货类与路径</label><textarea name="details" rows="5" placeholder="示例：锂电池/整车，深圳—阿拉木图—库雷克—巴库—伊斯坦布尔—德国；目标15天内"></textarea></div>
              <div style="margin-top:14px"><button class="btn btn-primary" type="submit">提交需求</button> <a class="btn btn-outline" href="mailto:contact@hpf.kz">或直接邮件：contact@hpf.kz</a></div>
            </form>
          </div>
          <div class="card">
            <h3>常见问答</h3>
            <p class="muted">• 带电产品如何合规？— 依据UN38.3/ADR与当地危化规范执行，提供一次性合规设计模板。<br/>• 红海/北部走廊波动怎么办？— 提供黑/里海冗余与多走廊备份方案。<br/>• 如何保障确定性？— 以SLA为核心，节点里程碑追踪、异常T+0预案与确定性保险。</p>
          </div>
        </div>
      </section>
    </main>

    <footer>
      <div class="container">© {new Date().getFullYear()} HPF.kz · TITR · Kuryk Green Channel · TIR Super Truck</div>
    </footer>
  </body>
</html>
